services:
  - type: web
    name: chesscom-style-chess
    env: node
    plan: free
    region: frankfurt
    rootDir: server
    buildCommand: "NPM_CONFIG_PRODUCTION=false npm run build:all"
    startCommand: "npm run start"
    healthCheckPath: "/api/health"
    autoDeploy: true
